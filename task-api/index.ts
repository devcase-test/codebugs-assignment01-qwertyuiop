import { getJoeFriends, getJoeFriendDetails } from './rest';

export const run = () => {
    throw new Error('Not implemented yet');
};

try {
    run();
} catch (error) {
    console.error(error);
}
